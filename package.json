{
  "name": "sotrudniki1",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack SPA for employee operations and analytics",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "lint": "npm run lint -ws",
    "test": "npm test -ws",
    "check:conflicts": "if rg --color never \"^(<{7}|={7}|>{7})\"; then echo 'Merge conflict markers found'; exit 1; else echo 'No conflict markers found'; fi"
  }
}
